--- a/gcc/cp/name-lookup.cc
+++ b/gcc/cp/name-lookup.cc
@@ -4667,2 +4667,3 @@
-      if ((partition_p && TREE_PUBLIC (ns))
-	  || (type && DECL_MODULE_EXPORT_P (type)))
+      if (type
+	  && ((partition_p && TREE_PUBLIC (ns))
+	      || DECL_MODULE_EXPORT_P (type)))
--- /dev/null
+++ b/gcc/testsuite/g++.dg/modules/reflect-4_a.C
@@ -0,0 +1,11 @@
+// PR c++/124200
+// { dg-do compile { target c++26 } }
+// { dg-additional-options "-fmodules -freflection" }
+// { dg-module-cmi M:Types }
+
+export module M:Types;
+
+export namespace N
+{
+  struct S {};
+}
--- /dev/null
+++ b/gcc/testsuite/g++.dg/modules/reflect-4_b.C
@@ -0,0 +1,23 @@
+// PR c++/124200
+// { dg-do compile { target c++26 } }
+// { dg-additional-options "-fmodules -freflection" }
+
+module;
+
+#include <meta>
+
+module M:Scanner;
+
+import :Types;
+
+consteval bool
+check ()
+{
+  const auto members = std::meta::members_of (
+    ^^N, std::meta::access_context::unchecked ());
+  return members.size () == 1
+    && members[0] == (^^N::S)
+    && std::meta::identifier_of (members[0]) == "S";
+}
+
+static_assert (check ());
+
+static_assert (check ());
